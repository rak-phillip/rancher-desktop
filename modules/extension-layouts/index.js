import path from 'path';

export default function() {
  const { nuxt } = this;

  // this.addLayout({
  //   name: 'preferences',
  //   src:  path.posix.join(nuxt.options.srcDir, 'pkg', 'rancher-desktop', 'layouts', 'preferences.vue'),
  // });
}
